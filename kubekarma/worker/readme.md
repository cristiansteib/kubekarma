# The worker that runs the tests
This directory must only contain the code related to the worker that runs the tests,
and the tests themselves.

Also, this directory is used to build the docker image that will be used by the worker.